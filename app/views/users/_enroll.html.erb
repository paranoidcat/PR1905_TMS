<%= form_for(current_user.user_course.build(course_id: @course.id)) do |f| %>
  <div><%= f.hidden_field :followed_id %></div>
  <%= f.submit "Enroll", class: "btn btn-large btn-primary" %>
<% end %>

